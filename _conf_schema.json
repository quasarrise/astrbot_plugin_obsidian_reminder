{
  "vault_path": {
    "description": "Obsidian 笔记库路径（任务功能总开关）",
    "hint": "留空则关闭全部任务扫描与推送功能",
    "type": "string"
  },
  "morning_push_time": {
    "description": "早间任务推送",
    "hint": "留空则不推送。格式 HH:MM，如 08:30",
    "type": "string"
  },
  "noon_push_time": {
    "description": "午间任务推送",
    "hint": "留空则不推送。格式 HH:MM，如 13:00",
    "type": "string"
  },
  "night_push_time": {
    "description": "晚间任务推送",
    "hint": "留空则不推送。格式 HH:MM，如 20:00",
    "type": "string"
  },
  "review_day": {
    "description": "周复盘日",
    "hint": "需同时设置时间。mon/tue/wed/thu/fri/sat/sun",
    "type": "string"
  },
  "review_time": {
    "description": "周复盘时间",
    "hint": "需同时设置复盘日。格式 HH:MM，如 22:00",
    "type": "string"
  },
  "news_prompt": {
    "description": "新闻 LLM 提炼提示词模板",
    "hint": "留空使用内置默认。{materials} 占位符会被替换为采集到的新闻素材。",
    "type": "text"
  },
  "review_prompt": {
    "description": "周复盘 LLM 提示词模板",
    "hint": "留空使用内置默认。{context} 占位符会被替换为本周任务清单。",
    "type": "text"
  },
  "news_enabled": {
    "description": "启用新闻推送",
    "type": "bool",
    "default": false
  },
  "morning_news_time": {
    "description": "早间新闻推送",
    "hint": "需先开启新闻推送开关。格式 HH:MM",
    "type": "string"
  },
  "night_news_time": {
    "description": "晚间新闻推送",
    "hint": "需先开启新闻推送开关。格式 HH:MM",
    "type": "string"
  },
  "news_groups": {
    "description": "新闻分组（最多3组，每组可添加多个 RSS 源）",
    "type": "template_list",
    "templates": {
      "news_group": {
        "name": "新闻分组",
        "hint": "添加一个新闻分组（最多3组）",
        "items": {
          "name": {
            "description": "分组名称（例：国内要闻、科技资讯）",
            "type": "string",
            "default": ""
          },
          "mode": {
            "description": "处理模式: llm=AI提炼, raw=原文链接整理",
            "type": "string",
            "options": ["llm", "raw"],
            "default": "llm"
          },
          "sources_text": {
            "description": "RSS 源（每行一条，格式：名称|网址）",
            "type": "text",
            "default": ""
          }
        }
      }
    },
    "default": []
  },
  "task_file": {
    "description": "每日任务文档路径模板（daily 模式下有效）",
    "hint": "{{date}} 替换为日期。例: {{date}}.md → 2026-06-25.md；也可写 inbox.md",
    "type": "string",
    "default": "{{date}}.md"
  },
  "task_mode": {
    "description": "任务写入模式",
    "hint": "daily: 写入 task_file 指定文档; docs: 写入下方配置的文档列表",
    "type": "string",
    "options": ["daily", "docs"],
    "default": "daily"
  },
  "task_format": {
    "description": "任务记录格式",
    "hint": "emoji: Tasks 插件格式; dataview: Dataview 插件格式",
    "type": "string",
    "options": ["emoji", "dataview"],
    "default": "emoji"
  },
  "task_docs": {
    "description": "任务写入目标文档（最多4个，不填则用上方 task_file 路径）",
    "type": "template_list",
    "templates": {
      "doc_entry": {
        "name": "任务文档",
        "hint": "配置一个 Obsidian 任务文档",
        "items": {
          "path": {
            "description": "文件路径（相对 vault，含 .md）",
            "type": "string",
            "default": ""
          },
          "trigger": {
            "description": "触发词（可选，留空则用文件名）",
            "type": "string",
            "default": ""
          }
        }
      }
    },
    "default": []
  }
}
